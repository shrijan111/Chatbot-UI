version: "3.9"

services:
  chatbot-ui:
    image: chatbot-ui:latest
    container_name: chatbot-ui
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
